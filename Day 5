int main()
{
    string n_temp;
    getline(cin, n_temp);

    int n = stoi(ltrim(rtrim(n_temp)));
 for(int i=1;i<=10;++i){  
   int ans=n*i;
   cout<<n<< " x " <<i<<" = "<<ans<<endl;
   
 }
    return 0;
   
}

string ltrim(const str
